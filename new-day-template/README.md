# Day 1

https://adventofcode.com/2025/day/1

## Instructions

> [!NOTE]
> In these instructions, replace `{n}` with the day number, e.g. `1` for Day 1.

1. Copy the `new-day-template` directory into `days/{n}`
2. In `days/{n}/package.json`:
  a. Change the `name` to `day-{n}`
3. In `days/{n}/README.md`:
  a. Change line 1 to `# Day {n}`
  b. Change line 3 to `https://adventofcode.com/2025/day/{n}`
  c. Delete these instructions
